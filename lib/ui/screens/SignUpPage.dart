import 'package:flutter/cupertino.dart';

class SignUpPagee extends StatelessWidget {
  const SignUpPagee({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
